import OurTeacher from "./OurTeacher"
import { Image, Box } from '@chakra-ui/react'

export default function OurTeacherPage({teachers}) {
    return (
        <OurTeacher teachers={teachers}/>
    )
}